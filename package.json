{
  "name": "quickstop",
  "version": "1.0.0",
  "description": "Este proyecto es una **Aplicación Web Progresiva (PWA)** que permite a los usuarios **buscar parkings cercanos**, **ver disponibilidad en tiempo real**, **realizar reservas**, **compartir disponibilidad de tus parkings** y **rebicir cobros y generar pagos**.   Todo ello cumpliendo con la **normativa europea de accesibilidad (EAA 2025)** y con una arquitectura moderna, escalable y profesional.",
  "main": "index.js",
  "private": true,
  "workspaces": [
    "app"
  ],
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "dev:db": "docker-compose up -d",
    "dev:app": "npm --workspace=app run dev",
    "dev:windows:api": "dotenv -e .env -- cmd /c \"cd api && mvnw spring-boot:run -DskipTests\"",
    "dev:api": "dotenv -e .env -- sh -c 'cd api && ./mvnw spring-boot:run -DskipTests'",
    "dev": "npm run dev:db && concurrently -n WEB,API -c auto \"npm run dev:app\" \"npm run dev:api\"",
    "dev:windows": "npm run dev:db && concurrently -n WEB,API -c auto \"npm run dev:app\" \"npm run dev:windows:api\"",
    "build:app": "npm run build --workspace=app",
    "build:api": "cmd /c \"cd api && mvnw clean install -DskipTests\"", 
    "build": "npm run build:app && npm run build:api",
    "build:windows": "npm run build:app && npm run build:windows:api",
    "start:app": "npm run preview --workspace=app",
    "start:api": "dotenv -e .env -- java -jar api/target/api-0.0.1-SNAPSHOT.jar"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alex-boni/QuickStop.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/alex-boni/QuickStop/issues"
  },
  "homepage": "https://github.com/alex-boni/QuickStop#readme",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "dotenv-cli": "^10.0.0"
  }
}
